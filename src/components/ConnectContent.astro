---
import { SITE, SOCIALS } from "@consts";
import Link from "@components/Link.astro";
---

<article class="animate space-y-4">
  <p>
    If you want to get in touch with me about something or just to say hi, reach
    out on social media or send me an email.
  </p>
  <p>
    Want a proper conversation? <Link href="https://www.cal.eu/junayed711"
      >Book time on my calendar →</Link
    >
  </p>
</article>
<ul class="animate not-prose flex flex-wrap gap-2">
  {
    SOCIALS.map((SOCIAL) => (
      <li class="flex gap-x-2 text-nowrap">
        <Link
          href={SOCIAL.HREF}
          external
          aria-label={`${SITE.TITLE} on ${SOCIAL.NAME}`}
        >
          {SOCIAL.NAME}
        </Link>
        {"/"}
      </li>
    ))
  }
  <li class="line-clamp-1">
    <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.TITLE}`}>
      {SITE.EMAIL}
    </Link>
  </li>
</ul>
